import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  checkmarkContainer: {
    position: 'absolute',
    height: '100%',
    alignSelf: 'center',
    justifyContent: 'center',
  },
});
