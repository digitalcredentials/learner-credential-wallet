import { StyleSheet } from 'react-native';
import { Color } from '../../styles/colors';

export default StyleSheet.create({
  studentIdPhoto: {
    width: '100%',
    aspectRatio: 1,
    resizeMode: 'contain',
  },
});
